using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class YouLoose : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Lock cursor + cursor visible
        Cursor.lockState = CursorLockMode.None;
        Cursor.visible = true;
    }

  
}
